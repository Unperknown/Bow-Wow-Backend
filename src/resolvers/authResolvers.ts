const authResolvers = {
  Mutation: {
    signup: async (_: any, { user }: any) => {
      return <any>[];
    },
    login: async (_: any, { id, password }: any) => {
      return <any>[];
    }
  }
}

export {
  authResolvers,
}